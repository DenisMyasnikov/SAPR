#include "listrods.h"

ListRods::ListRods(int i)
{
//    for (int k=0; k<=i; k++)
//        rods.push_back(i+1);
}

Rod ListRods::getRod(int id)
{
//    if (!rods.isEmpty()){
//        for (int i = 0; i <= rods.size(); i++){
//            if (rods[i].getId() == id)
//                return rods[i];
//        }
//    }
}

